---
applyTo: ".changeset/**/*.md"
---

# Changeset review rules

Do not flag the casing of "css modules" / "CSS Modules" / "css-modules" in
changeset summaries. All variants are acceptable — treat them as equivalent and
do not suggest normalizing the capitalization or hyphenation.
