import { Badge } from "@/components/Badge/badge";

export default {
  component: Badge,
  title: "Badge",
  tags: ["badge"],
};

export const Default = {
  args: {
    text: "experiment",
    state: "success",
  },
};
