import { Icon } from "../Icon/Icon";
import {ICONS_MAP} from '../Icon/IconCommon'
import Flags from "./Flags";
import Payments from './Payments'
import { Meta, IconGallery, IconItem } from "@storybook/blocks";
import { ThemeProvider } from "../../theme";

<Meta title='Display/Icon/Icons' />

# Glyphs

<ThemeProvider theme='light'>
  <IconGallery>
    {Object.keys(ICONS_MAP).map((iconName) => (
      <IconItem name={iconName} key={iconName}>
        <Icon name={iconName} />
      </IconItem>
    ))}
  </IconGallery>
</ThemeProvider>

# Flags

<IconGallery>
  {["usa", "de", "sg", "uk", "ie", "eu", "in", "nl", "au", "br", "ca", "za"].map((iconName) => (
    <IconItem name={iconName} key={iconName}>
      <Flags name={iconName} />
    </IconItem>
  ))}
</IconGallery>

# Payments

<IconGallery>
  {["visa", "mastercard", "amex", "paypal"].map((iconName) => (
    <IconItem name={iconName} key={iconName}>
      <Payments name={iconName} />
    </IconItem>
  ))}
</IconGallery>
