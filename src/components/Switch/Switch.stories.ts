import { Switch } from "./Switch";

export default {
  component: Switch,
  title: "Switch",
  tags: ["switch"],
};

export const Default = {
  args: {
    checked: true,
    disabled: false,
  },
};
