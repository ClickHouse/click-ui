import { Accordion } from "./Accordion";

export default {
  component: Accordion,
  title: "Accordion",
  tags: ["accordion"],
};

export const Default = {
  args: {
    title: "Accordion title",
    size: "medium",
  },
};
