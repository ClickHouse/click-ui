---
"@clickhouse/click-ui": patch
---

Enlarge the `check` icon glyph so it fills more of its viewBox and matches the
stroke weight of the other icons. The `Checkbox` renders its check at a
compensated size, so the checkmark inside the box looks unchanged.
