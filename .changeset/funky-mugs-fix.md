---
'@clickhouse/click-ui': minor
---

Make ButtonGroup component support multiple selection. Provides controlled (consumer handles state) and uncontrolled support (state managed by component for consistent behaviour for cases not requiring control)
