---
'@clickhouse/click-ui': patch
---

Make mouse-only controls keyboard operable: file upload dropzones, selectable cards, date range grids, the date picker trigger, and confirmation dialog initial focus. Also restore DatePicker weekday headers (`weekDays`) so calendars render.
