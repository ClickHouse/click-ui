---
'@clickhouse/click-ui': patch
---

Add `wrap` prop to Text to control text wrapping: `wrap`, `nowrap`, `break-word` or `break-all`. When unset the text inherits the wrapping behaviour of its parent.
