---
'@clickhouse/click-ui': minor
---

Enable year and month selection in `DateRangePicker` by clicking on the date in the calendar header. This matches the existing `DatePicker` behavior and applies to both the standalone calendar and the "Custom time period" calendar shown alongside `predefinedDatesList`.
