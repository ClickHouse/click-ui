---
'@clickhouse/click-ui': patch
---

Fix Alert dropping a consumer-provided `className`. The root element applied its own class after spreading the rest props, overwriting any incoming `className` (including the class generated by `styled(Alert)`). The consumer `className` is now merged onto the root, and `className` is a supported prop.
