---
'@clickhouse/click-ui': patch
---

Bump all `@radix-ui/react-*` dependencies to the versions shipped in Radix `1.6.7`, and de-duplicate the resulting transitive tree.

Toasts no longer carry `role="status"`. Select them with `li[data-state="open"]`.
