---
'@clickhouse/click-ui': patch
---

Add the missing BEM base class to Accordion, Alert, Collapsible, and Link so every block and element has a base for its modifiers to modify. No change in behavior.
